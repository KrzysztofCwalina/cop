# Negative: non-existent field access produces no match
# @expect no-output

import csharp-checks
predicate wrongField(Type) => Type.FakeField:equals('x')
command MAIN = foreach csharp.Types:wrongField => '{item.Name}'
