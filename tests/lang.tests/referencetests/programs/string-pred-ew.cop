# String predicate: ew (ends with)
# @expect output-contains: GoodClient

import csharp-checks
predicate nameEndsClient(Type) => Type.Name:endsWith('Client')
command MAIN = foreach csharp.Types:nameEndsClient => '{item.Name}'
