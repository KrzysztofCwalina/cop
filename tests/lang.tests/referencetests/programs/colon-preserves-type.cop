# Colon operator: after filtering, items retain type — can still access .Methods.Count
# @expect output-contains: methods

import csharp-checks
command MAIN = foreach csharp.Types:isPublic => '{item.Name} has {item.Methods.Count} methods'
