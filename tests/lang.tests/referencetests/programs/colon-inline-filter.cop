# Colon operator: inline field filter — predicate using Name:sw
# @expect output-contains: GoodClient

import csharp-checks
predicate nameStartsGood(Type) => Type.Name:startsWith('Good')
command main = foreach csharp.Types:nameStartsGood => '{item.Name}'
