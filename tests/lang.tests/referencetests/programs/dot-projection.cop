# Dot operator: projection on collection — Methods.Name produces list of strings
# @expect output

import csharp-checks
predicate hasMethodNames(Type) => Type.Methods.Name.Count > 0
command MAIN = foreach csharp.Types:hasMethodNames => '{item.Name}'
