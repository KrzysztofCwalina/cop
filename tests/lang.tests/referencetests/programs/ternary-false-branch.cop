# Ternary: false branch — sealed types excluded, Placeholder should still be present
# @expect output
# @expect output-contains: Placeholder

import csharp-checks
predicate notSealed(Type) => isSealed ? false | true
command main = foreach csharp.Types:notSealed => '{item.Name}'
