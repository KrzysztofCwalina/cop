# Dot operator: chained access — type.Methods.Count
# @expect output

import csharp-checks
predicate hasMethods(Type) => Type.Methods.Count > 0
command MAIN = foreach csharp.Types:hasMethods => '{item.Name}'
