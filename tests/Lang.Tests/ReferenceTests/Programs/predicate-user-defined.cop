# Predicates: user-defined combining built-in predicates
# @expect output-contains: GoodClient

import csharp-checks
predicate isSealdGood(Type) => isSealed && Type.Name:startsWith('Good')
command main = foreach csharp.Types:isSealdGood => '{item.Name}'
