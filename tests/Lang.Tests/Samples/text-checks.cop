## No print() calls in production Python code
foreach Lines:python:matches('\bprint\s*\('):!pathMatches(item.File.Path, '**/tests/**') => '{warning:@yellow} print() should not be used in production code'
