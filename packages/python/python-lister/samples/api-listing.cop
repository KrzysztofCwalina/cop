import python-library-checks

# print API surface
export command api-listing = foreach Code.Api:isPython:isPublicApi => '{item.Signature}'

# save API surface to a file
let apiText = Code.Api:isPython:isPublicApi.text('{item.Signature}')
export command save-api = save('api-surface.txt', apiText)
