# Bandit security scanner integration — runs bandit and produces cop diagnostics
# Prerequisites: bandit must be installed (pip install bandit)
# Usage: import python-bandit

import code-analysis

let data = provider('python-bandit', nic)

## Returns all bandit diagnostics.
export function diagnostics() : [Diagnostic] => data.Diagnostics

## All bandit security checks as a collection.
export let bandit-checks = data.Diagnostics

command main = foreach bandit-checks => '{item.FilePath}({item.Line}): {item.Severity}: {item.RuleId}: {item.Message}'
