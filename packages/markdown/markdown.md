# markdown

Provides types for analyzing markdown documents: headings, links, sections, and fenced code blocks.

## Usage

```cop
import markdown

let allHeadings = Markdown.Headings
let codeBlocks = Markdown.FenceBlocks:markdown
```
