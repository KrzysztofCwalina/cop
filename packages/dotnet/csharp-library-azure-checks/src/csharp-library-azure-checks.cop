# Azure SDK client library checks — all combined

import code-analysis

export let csharp-library-azure-checks = client-design + options-design + naming + method-design + code-quality

command MAIN = CHECK(csharp-library-azure-checks)
