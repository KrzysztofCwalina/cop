import csharp
import python
import javascript

# Combine types from all three language providers into one collection
let allTypes = csharp.types() + python.types() + javascript.types()

command MAIN = foreach allTypes:isPublic => '{Type.Name} ({Type.Kind}) - {Type.File.Language}'
