import files

# Find empty folders and large files
let large-files = files.files():where(File.Size > 1000000)

command main =
    foreach files.folders():empty => 'Empty: {Folder.Path}' |
    foreach large-files => 'Large: {File.Path} ({File.Size} bytes)'
