let names = ['Alice' 'Bob' 'Charlie' 'Diana']
let nameCount = names.Count
let firstName = names.First

print('{nameCount} people, first is {firstName}')

command MAIN = foreach names => 'Hello, {item}!'
