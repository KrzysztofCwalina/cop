import http

# Route requests: answer /chat, otherwise 404.
function handle(Request) =>
    Request.Uri:equals('/chat') ? ok(Request, 'chat ready') : notFound

async foreach requests() => handle => responses()
