# Spectral integration — runs Spectral and produces cop Violations
# Prerequisites: Spectral must be installed

import code

let data = provider('analysis-spectral')

## All Spectral violations as a collection.
export let spectral-checks = data.Violations

command MAIN = CHECK(spectral-checks)
